# `ExtractionYield`

A yield from the extraction operation.

## Properties

- `symbol` **[TradeSymbol](../schemas/TradeSymbol.md)** *(required)*
- `units` **integer** *(required)* — The number of units extracted that were placed into the ship's cargo hold.
