# `ShipRegistration`

The public registration information of the ship

## Properties

- `name` **string** *(required)* — The agent's registered name of the ship
- `factionSymbol` **string** *(required)* — The symbol of the faction the ship is registered with
- `role` **[ShipRole](../schemas/ShipRole.md)** *(required)*
