# `JumpGate`

Details of a jump gate waypoint.

## Properties

- `symbol` **[WaypointSymbol](../schemas/WaypointSymbol.md)** *(required)*
- `connections` **string[]** *(required)* — All the gates that are connected to this waypoint.
