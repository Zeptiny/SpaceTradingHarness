# `ShipRequirements`

The requirements for installation on a ship

## Properties

- `power` **integer** — The amount of power required from the reactor.
- `crew` **integer** — The number of crew required for operation.
- `slots` **integer** — The number of module slots required for installation.
